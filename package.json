{
	"name": "kube-demo",
	"version": "0.0.0",
	"private": true,
	"devDependencies": {
		"@types/cors": "^2.8.17",
		"@types/express": "^4.17.21",
		"@types/node": "^20.11.25",
		"esbuild": "^0.20.1",
		"nodemon": "^3.1.0",
		"prettier": "^3.2.5",
		"typescript": "^5.4.2"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.18.3"
	},
	"scripts": {
		"docker:auth": "echo $GITHUB_TOKEN | docker login docker.pkg.github.com -u raine-works --password-stdin",
		"docker:build": "docker build -t docker.pkg.github.com/raine-works/kube-demo/worker:latest --build-arg PORT=8080 .",
		"docker:push": "docker push docker.pkg.github.com/raine-works/kube-demo/worker:latest",
		"build": "node esbuild",
		"dev": "nodemon",
		"start": "node .build"
	}
}